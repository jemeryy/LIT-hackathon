# Session Handoff — PS4 build scope grilled, plan locked with Codex, OpenRouter key tested

## Where it started
Jeremy asked for: read the previous handoff, grill the build scope from the demo script and the
wireframe generator, run plan-with-codex. Then he added the OpenRouter key to `.env` and asked which
model to use. He then asked for this handoff and said the build starts next session.
Session ran Sat 5 Sep 2026 ~15:55 to 16:30 SGT. Hard deadline Sun 6 Sep 12:00 SGT.

## Important discoveries and decisions
- Self-grill produced 20 locked decisions in
  `C:\Users\jemer\code\hackathon\notes\grill-2026-09-05-build-scope.md`. Headline calls: FastAPI +
  one static `index.html`; model only extracts facts with exact quotes and writes three short texts
  (story, 500-char summary, written request); every score, rank, gate, gap, blind spot and timeline
  is a rule in code; one server-rendered PNG viewer with overlay boxes for PDF, image and video (no
  pdf.js); no statute search (a `statutes.json` with the four gate sections quoted verbatim); no DB,
  no auth, no deploy; one `selfcheck.py` instead of a test suite; sample pack generated by script.
- Codex debate changed one grill call: text read from a picture is no longer capped at Weak. It
  sets a `needs_check` flag instead. Needed so the bank transfer screenshot ranks Strong as the
  wireframe shows. Codex converged in round 2, no blocking.
- Plan locator format: normalised 0-1 coordinates for every locator (PDF, image, video frame),
  front end places rectangles with percentages.
- OpenRouter: the key in `.env` has a $15 cap (checked via `/api/v1/key`). OpenRouter accepts the
  Anthropic Messages format at `https://openrouter.ai/api/v1/messages`, so
  `anthropic.Anthropic(api_key=OPENROUTER_API_KEY, base_url="https://openrouter.ai/api")` works with
  no code change. Tested: a tool call with `tool_choice` plus a base64 image on
  `anthropic/claude-sonnet-5` returned a correct tool_use (666 in / 71 out tokens).
- Model decision: `anthropic/claude-sonnet-5` for every call. A full sample-pack run is about
  $0.10, so ~100 real runs on the cap. Develop with fixtures (`USE_FIXTURES=1`), real calls only for
  dry runs and the demo.
- Codex critique found the wireframe rank order can be reproduced exactly by a frozen
  strength+category table (deposit_terms, deposit_paid, handover_acceptance, moveout_condition,
  damage_allegation, movein_condition). It is in the plan's Approach section.
- Tooling confirmed installed: Python 3.12, anthropic 0.103, fastapi, uvicorn, pdfplumber, PyMuPDF,
  Pillow, pytesseract + tesseract.exe on PATH, openpyxl, reportlab, python-docx, httpx, ffmpeg on
  PATH. Codex CLI reachable (`python tools/codex_exec.py --pong` prints PONG).

## Decisions locked + what shipped
- Plan locked: `C:\Users\jemer\code\hackathon\plans\2026-09-05-ps4-case-builder-build.md`
  (status: active, 8 high-level steps with lanes A backend / B front end / C legal / J Jeremy,
  minutes per step, cut-if-over per step, JSON contract, Codex transcripts in Notes). Executor:
  Claude. Patched after the key landed: Constraints, the `llm.py` bullet, Open question 1, Changelog.
- Grill file: `C:\Users\jemer\code\hackathon\notes\grill-2026-09-05-build-scope.md`.
- `C:\Users\jemer\code\hackathon\.env` now holds `OPENROUTER_API_KEY` (Jeremy),
  `ANTHROPIC_BASE_URL=https://openrouter.ai/api` and `MODEL=anthropic/claude-sonnet-5` (this session).
  `.env` is gitignored.
- `C:\Users\jemer\code\hackathon\notes\demo-script.md` line 36: the stub sentence now says statute
  text is only the sections the gate links plus the court guide (was "~40 sections").
- Nothing committed this session. `git status`: modified `notes/demo-script.md`, untracked
  `notes/grill-2026-09-05-build-scope.md`, `plans/`, and this handoff.
- Assumed and not confirmed: app name "Case Builder", folder `builds/case-builder/`, team split
  3 dev + 2 legal.

## Key files for next session
- Plan file: `C:\Users\jemer\code\hackathon\plans\2026-09-05-ps4-case-builder-build.md` — read
  first. Step 1 (scaffold + JSON contract + fixture `llm.py`) unblocks every lane.
- `C:\Users\jemer\code\hackathon\notes\grill-2026-09-05-build-scope.md` — the 20 locked decisions.
- `C:\Users\jemer\code\hackathon\notes\demo-script.md` — build only what it touches.
- `C:\Users\jemer\code\hackathon\notes\wireframe\gen.py` — the seven screens the front end copies.
- `C:\Users\jemer\code\hackathon\research\raw\fara-reuse.md` — fara paths to copy
  (`C:\Users\jemer\code\fara\business\fara\tools\doc_extract.py` for `_extract_pdf` and locators).
- `C:\Users\jemer\code\hackathon\.claude\skills\plan-with-codex\SKILL.md` — execute phase rules
  (executor checkpoint, then `review-with-codex` after the build).
- Memory files touched: none.

## Running state
- Background processes: none. The skill-opus planning agent (id a7cbc9f9187950bf8) finished; it
  used one Codex session (fresh call + one `--resume-last`). A new Claude session must start Codex
  fresh (no `--resume-last`).
- Dev servers / ports: none.
- Open worktrees / branches: `main` at `C:\Users\jemer\code\hackathon`, clean apart from the files
  above. Remote origin https://github.com/jemeryy/LIT-hackathon.git.

## Verification — how to confirm things still work
- `python tools/codex_exec.py --pong` in `C:\Users\jemer\code\hackathon` — prints PONG.
- `grep -E "^(ANTHROPIC_BASE_URL|MODEL)=" .env` — shows the OpenRouter base URL and
  `anthropic/claude-sonnet-5`.
- Key check (free): GET `https://openrouter.ai/api/v1/key` with `Authorization: Bearer <key>` —
  shows `limit: 15` and usage so far (about $0.01 after this session's two probes).
- `cd notes/wireframe; python gen.py` — prints `wrote 7 artboards`.

## Deferred / pending tasks + open questions
- Deferred: the whole build. Nothing under `builds/` exists yet. Jeremy said execute next session.
- Deferred: commit of the plan, grill file, demo-script edit and this handoff. Commit when Jeremy
  says.
- Deferred: SCTA 1984 s 2, s 5 and Schedule quoted verbatim into `content/statutes.json` (lane C,
  fetch `https://sso.agc.gov.sg/Act/SCTA1984?ViewType=Pdf` with a browser user agent, 6 s between
  requests).
- Open: app name (assumed "Case Builder").
- Open: lane assignments (who is A / B / C).

## Plan
`C:\Users\jemer\code\hackathon\plans\2026-09-05-ps4-case-builder-build.md`, status active,
executor Claude. Sequence: scaffold + contract + fixtures -> E1 clause-4 highlight slice -> rest of
sample pack -> intake + upload + parse -> extraction with quote matching -> gate + statute panel ->
evidence rank + viewer -> gaps, blind spots, timeline -> next steps + xlsx + claim pack zip + written
request -> front end wires seven screens -> `selfcheck.py` -> `review-with-codex` -> deck. Feature
freeze Sun 10:30, submit by 11:30.

## Next steps
1. Read the plan. Run the execute-start executor checkpoint from the plan-with-codex skill (plan
   says Claude).
2. Execute step 1 (scaffold `builds/case-builder/`, JSON contract, sample `case.json`, fixture
   `llm.py`, `.env` loader). Then step 2 (E1 highlight slice) before anything else.
3. Keep `USE_FIXTURES=1` during development. Real model calls only for dry runs.
4. After the build: `review-with-codex`, then the deck (`make-slides`), then Devpost by Sun 12:00
   SGT with Jeremy's green light.

## Next step (for the next session after /clear)
Next step: read C:\Users\jemer\code\hackathon\handoffs\handoff-2026-09-05-ps4-plan-locked.md, then execute plans/2026-09-05-ps4-case-builder-build.md from step 1 (executor Claude, fixtures on).
